<header>
  <div class="topnav">
      <!-- H1 LOGO -->
      <a class="logo" href="/" style="text-decoration: none"><h1>Yeah!</h1></a>
      <!-- BARRA SEARCH -->
      <div class="search-container">
        <form action="/search" method="GET">
          <input
            class="barra-search"
            type="text"
            placeholder="Buscar"
            name="search"
            value= <%= (typeof laQuery != 'undefined')? laQuery : null %>
          >
          <button class="lupa" type="submit" >
            <img src="/images/search-solid.svg" class="imgLupa"/>
          </button>
        </form>
      </div>
      <!-- LOGIN REGISTER CART -->
      <div class="log-reg-cart">
          <% if(typeof usuarioLogeado == "undefined") { %>
        <a href="/users/login"><button class="login" name="login">Iniciá sesión</button></a>
        <a href="/users/register"><button class="register" name="register">Creá una cuenta</button></a>
        </button></a>
        <% } else  { %>
          <% if (usuarioLogeado != "undefined" ) { %>
            <a href="/users/admin/"><span>Crear Producto</span></a>
            <% } %>
        <div class="usLog">
          <a id="usLogName" href="/users/perfil/<%= usuarioLogeado.id %>" ><%= usuarioLogeado.username %>
            <img class="usLog" id="usLogImg" src="/uploads/avatars/<%= usuarioLogeado.avatar %>"">
          </a>
          
            <div class="usMenu">
              <button type="submit" id="closeSessionButton" onclick="closeSession('<%= usuarioLogeado.id %>')">Cerrar sesión</button>
            </div>
            <a href="/products/cart"><button class="cart" name="cart">
              <img class="cartImg" src="/images/shopping-cart-solid.svg"/>
       </div>
       <% } %>
        
      </div>
    </div>
</header>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>