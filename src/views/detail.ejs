<!DOCTYPE html>
<html lang="en">
    <head>
        <script
        src="https://kit.fontawesome.com/712575d4a5.js"
        crossorigin="anonymous"
        ></script>
        <title>Yeah!</title>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/stylesheets/stylex.css" />
        <link rel="stylesheet" href="/css/detail.css" />
        <link rel="shortcut icon" href="/images/logo-y-title.png" />
      </head>
<body>
    <%- include('partials/header') %> 
    <main>
        <section class="productDetail">
            <div class="containerProduct">
                <ul>
                    <li><img src="/uploads/products/<%= product.imgpath %>" ></li>
                    <li><ul style="flex-direction: column;">
                        <li><h1 id="nameProduct"><%= product.name %></h1></li>
                        <li><h3 id="priceProduct">Precio: $ <%= new Intl.NumberFormat().format(product.price) %></h3></li>
                        <li><h3 id="descriptionProduct">Descripción: <%= product.description %></h3></li>
                        <li><h3 id="stockProduct">Stock: <%= product.stock %></h3></li></li>
                    </ul>
                </ul>
                
            </div>

            <div class="controls">
                <% if(typeof usuarioLogeado == "undefined") { %>
                    <% } else  { %>
                <% if (usuarioLogeado.admin == 1) { %>
                <a style="cursor:pointer" href="/edit/<%= product.id %>"><button>Editar</button></a>
                <form action="/delete/<%= product.id %>?_method=DELETE" method="POST">
                    <button type="submit">Borrar</button>
                    <% } %>
                <% } %>
                </form>
            </div>
        </section>
    </main>
    <%- include('partials/footer') %> 
</body>
</html>